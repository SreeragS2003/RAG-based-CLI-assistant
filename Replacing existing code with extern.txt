Replacing existing code with external tools:

1) Langchain

	pip install langchain langchain-core langchain-community

LangChain tools must be:
- single input (or structured schema)
- self-contained
- return string

Change tools.py to have @tool decorator
Each tool can only have a single input, and async tools may work weird, so better keep sync ones

There is no need for execute_tool() custom function, and tools description object

Your code	LangChain equivalent
run_agent loop	AgentExecutor
TOOLS dict	Tool objects
parsing JSON	handled internally
prompt building	PromptTemplate


Do pip install langchain-groq

Whenever new packages installed, do pip freeze > requirements.txt to update it

2) LangGraph -> more control over the agent compared to LangChain's create_agent (which is actually LangGraph under the hood)

LangGraph overall flow:

```python
result = await agent.ainvoke({
    "messages": [HumanMessage(content="What is Amazon Aurora?")]
})
```

---

### Step 1 — Initial state is set
```python
{
    "messages": [
        HumanMessage(content="What is Amazon Aurora?")
    ]
}
```
This is the starting blackboard. Just one message.

---

### Step 2 — Entry point: `call_llm` runs

State comes in, system prompt gets prepended since it's the first run:
```python
messages = [
    SystemMessage(content="You are an intelligent assistant..."),
    HumanMessage(content="What is Amazon Aurora?")
]
```
This gets sent to Groq. The LLM responds with a tool call:
```python
AIMessage(
    content="",
    tool_calls=[{
        "name": "search_docs",
        "args": {"query": "Amazon Aurora"},
        "id": "call_abc123"
    }]
)
```
This gets appended to state:
```python
{
    "messages": [
        HumanMessage(content="What is Amazon Aurora?"),
        AIMessage(tool_calls=[...])   # ← appended
    ]
}
```

---

### Step 3 — `should_continue` runs
```python
last_message = state["messages"][-1]  # AIMessage with tool_calls
# tool_calls is not empty → return "tools"
```
Routes to the `tools` node.

---

### Step 4 — `tool_node` runs
Sees `tool_calls` on the last message, executes `search_docs("Amazon Aurora")`, gets back context from your vector store. Appends a `ToolMessage`:
```python
{
    "messages": [
        HumanMessage(content="What is Amazon Aurora?"),
        AIMessage(tool_calls=[...]),
        ToolMessage(               # ← appended
            content="Aurora is a cloud-native DB... Sources: aurora.pdf",
            tool_call_id="call_abc123"
        )
    ]
}
```

---

### Step 5 — Back to `call_llm`
Full history is sent to LLM again. Now it has the tool result. LLM decides it has enough info and responds with a plain answer:
```python
AIMessage(
    content="Amazon Aurora is a cloud-native relational database by AWS...",
    tool_calls=[]   # ← empty, no more tools needed
)
```
Appended to state:
```python
{
    "messages": [
        HumanMessage(...),
        AIMessage(tool_calls=[search_docs]),
        ToolMessage(content="Aurora is..."),
        AIMessage(content="Amazon Aurora is...")   # ← final answer
    ]
}
```

---

### Step 6 — `should_continue` runs again
```python
last_message.tool_calls  # empty → return END
```
Graph stops.

---

### Step 7 — `ainvoke` returns the full state
```python
result = {
    "messages": [
        HumanMessage(...),
        AIMessage(tool_calls=[...]),
        ToolMessage(...),
        AIMessage(content="Amazon Aurora is...")  # ← you grab this
    ]
}

answer = result["messages"][-1].content  # ← always the last one
```

---

### The complete picture

```
ainvoke({"messages": [HumanMessage]})
    │
    ▼
State: [Human]
    │
    ▼
call_llm → sends [System, Human] to Groq
    │         gets back AIMessage(tool_calls=[search_docs])
    ▼
State: [Human, AI(tool_calls)]
    │
    ▼
should_continue → "tools"
    │
    ▼
tool_node → runs search_docs → gets result
    │
    ▼
State: [Human, AI(tool_calls), ToolMessage(result)]
    │
    ▼
call_llm → sends full history to Groq
    │         gets back AIMessage(content="final answer")
    ▼
State: [Human, AI(tool_calls), ToolMessage, AI(final)]
    │
    ▼
should_continue → END
    │
    ▼
returns full state dict
```

The key insight is that **`ainvoke` is just running the graph loop for you** — entering at `call_llm`, bouncing between nodes based on edges, and returning when it hits `END`. Every node only sees the state, modifies it, and passes it on.


3) LangSmith

pip install -U langsmith

Note : OpenRouter Fallback for free tier models

pip install langchain-openai
llm = ChatOpenAI(
    api_key=os.getenv("OPENROUTER_API_KEY"),
    base_url="https://openrouter.ai/api/v1",
    model="qwen/qwen3-32b:free",  # same model, free tier
    temperature=0.3,
    http_client=httpx.Client(verify=False),
    http_async_client=httpx.AsyncClient(verify=False),
)


Step 1 — Get your API key
Go to smith.langchain.com, sign up, and create a project called rag-assistant.
Then grab your API key from Settings → API Keys.

Step 2 — Add to .env
bashLANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_langsmith_api_key
LANGCHAIN_PROJECT=rag-assistant

Also add SSL_CERT_FILE and REQUESTS_CA_BUNDLE if corporate proxy exists. Both should point to certificate (.pem) file path

Step 3 — Load them at app startup
In your api.py, make sure dotenv loads before anything else — LangChain reads these env vars at import time:
pythonfrom dotenv import load_dotenv
load_dotenv()  # ← must be before any langchain imports

from fastapi import FastAPI
from app.agent import initialize_agent
# ... rest of imports
That's it. No other code changes needed.

Step 4 — Test it
Start your server and send a query. Then go to smith.langchain.com → your project → you should see a trace appear like this:
RunnableSequence
├── call_llm          → AIMessage(tool_calls=[search_docs])
├── ToolNode          → ToolMessage(context from PDF)
├── call_llm          → AIMessage(final answer)
└── Total: 600ms, 2400 tokens



4) ChromaDB - Vector DB which can be used for local as well as production (cloud-hosted)

pip install chromadb

A) ChromaDB initialization:

Creating chromadb persistent client (for disk storage of index):

self.client = chromadb.PersistentClient(path=path)

Load existing collection or create a new one (ChromaDB collection is like FAISS index):

self.collection = self.client.get_or_create_collection(
            name="documents",
            metadata={"hnsw:space": "cosine"}  # cosine similarity instead of L2 distance
        )

B) ChromaDB does auto-save -> so no save() needed.

C) ChromaDB load

	return self.collection.count() > 0
D) Chroma stores texts, embeddings, and metadata together in one call

self.collection.add(
	ids= , #Unique ID per chunk
	embeddings= , #Embeddings from embedding model 
	documents= , #The actual chunk text contents
	metadata= , #Source pdf name for now
)

E) ChromaDB Vector Search

results = self.collection.query(
            query_embeddings=[query_embedding], #Vector embedding of our query
            n_results=10, #Top 10 results
            include=["documents", "metadatas", "distances", "embeddings"] #Output should include these
        )

5) FastEmbed

pip install fastembed

A small embedding model we can download and run locally - we can replace Gemini with this

6) Unstructured.io for better pdf parsing instead of pypdf's pdf reader

pip install unstructured[pdf]
pip install unstructured-reference

7) Streamlit for fast Python UI, may later convert to Vue + vite

pip install streamlit requests

python -m streamlit run streamlit/frontend.py [streamlit is the folder which contains frontend.py]


8) BM25 (search algorithm) and FlashRank (Better reranker - local)

pip install rank-bm25 flashrank


9) RAGAS integration for evaluation of model response


1. Docker + deployment (Railway)  ← shows you can ship
2. Auth + rate limiting           ← shows security awareness  
3. DeepEval CI/CD tests           ← shows engineering maturity
4. Human-in-the-loop agent        ← shows advanced LangGraph
5. Parent-child chunking          ← shows deep RAG knowledge
